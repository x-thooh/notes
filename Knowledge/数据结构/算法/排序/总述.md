---
tags:
  - note/standard
  - type/standard
  - status/growing
  - topic/Algorithm
  - priority/Lv1
date: 2026-06-01 20:54
updated: 2026-06-01 20:54
status: growing
topic: Algorithm
priority: Lv1
deck: Note::Algorithm
---
# 总述
## 概念
假设含有`n`个记录的序列为$\{n_1，r_2,……,r_n\}$,其相应的关键字分别为$\{k_1,k_2,…,k_n\}$,需确定`1,2，…，n`的一种排列$P_1,P_2,…,P_n$,使其相应的关键字满足$k_1≤k_2≤…≤k_n$(非递减或非递增)关系，即使得序列成为一个按关键字有序的序列$\{r_{p1},r_{p2},…,r_{pn}\}$`,这样的操作就称为排序。

### 稳定排序
假设$k_i=kj(1≤i≤n,1≤j≤n,i\ne{j})$,且在排序前的序列中$r_1$领先于$r_j$(即i<j)。如果排序后$r_1$仍领先于$r_j$，则称所用的排序方法是稳定的；反之，若可能使得排序后的$r_j$
序列中领先$r_i$，则称所用的排序方法是不稳定的。

### 原地排序
原地排序就是指在排序过程中不申请多余的存储空间，只利用原来存储待排数据的存储空间进行比较和交换的数据排序。

## 比较
| 名称     | 最好/最坏/平均                 | 原地排序 | 稳定排序 |
| ------ | ------------------------ | ---- | ---- |
| 直接插入排序 | O(n)/O(n^2)/O(n^2)       | Y    | Y    |
| 希尔排序   | O(nlogn)                 | Y    | N    |
| 简单选择排序 | O(n^2)/O(n^2)/O(n^2)     | Y    | N    |
| 堆排序    | O(nlogn)                 | Y    | N    |
| 冒泡排序   | O(n)/O(n^2)/O(n^2)       | Y    | Y    |
| 快速排序   | O(nlogn)/O(n^2)/O(nlogn) | Y    | N    |
| 归并排序   | O(nlogn)                 | N    | Y    |

## 区别
### 冒泡排序&插入排序
冒泡排序不管怎么优化，元素交换的次数是一个固定值，是原始数据的逆序度。插入排序是同样的，不管怎么优化，元素移动的次数也等于原始数据的逆序度。
冒泡排序的数据交换要比插入排序的数据移动要复杂，冒泡排序需要 3 个赋值操作，而插入排序只需要 1 个。

### 归并排序&快速排序
归并排序的处理过程是由下到上的，先处理子问题，然后再合并。而快排正好相反，它的处理过程是由上到下的，先分区，然后再处理子问题。归并排序虽然是稳定的、时间复杂度为 O(nlogn) 的排序算法，但是它是非原地排序算法。我们前面讲过，归并之所以是非原地排序算法，主要原因是合并函数无法在原地执行。快速排序通过设计巧妙的原地分区函数，可以实现原地排序，解决了归并排序占用太多内存的问题。

## 延伸
[十大经典排序算法](http://ddrv.cn/docs/SortingAlgorithm/6.quickSort.html)
[八大排序算法（Java版）](https://mp.weixin.qq.com/s/2u4lV3zn-LFxvihM9PFF-g)